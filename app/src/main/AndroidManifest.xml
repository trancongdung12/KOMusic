<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.komusic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KOMusic">
        <activity android:name=".Register"></activity>
        <activity android:name=".Search"></activity>
        <activity android:name=".Login"></activity>
        <activity android:name=".Navigation.HandleNavigation"></activity>
        <activity android:name=".CollectionActivity"></activity>
        <activity
            android:name=".Player"
            android:theme="@style/FullScreen"></activity>
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>